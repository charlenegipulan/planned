<h1> Edit entry </h1>

<div class='form_panel'>
    <%= form_for @entry do |f| %>
        <br>
        <%= f.text_area :content %>
        <%= hidden_field_tag :offset, @offset %>
        <br>
        <%= f.submit 'Update' %>
    <% end %>
    
</div>